const changeUnitBtn = document.querySelector('.change-unit');
const background = document.querySelector('.change-unit__background');

changeUnitBtn.addEventListener('click', () => {
  let activeUnit = document.querySelector('.active');
  
  if (activeUnit.dataset.unit == 'celsius') {
    
  }
});